# OpenWiki — Team Context

You are operating inside an OpenWiki multi-user workspace. Each user has an isolated git worktree.

## Repository Conventions
- Use clear, descriptive commit messages
- Document decisions in markdown files
- Do not run destructive shell commands (rm -rf, DROP TABLE, etc.)
- Do not modify files outside the current worktree

## Output Format
- Prefer markdown for documentation
- Use code blocks with language hints for all code
- Keep responses concise and actionable
